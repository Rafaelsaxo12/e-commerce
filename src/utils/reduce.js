const numbers = [2, 4, 6, 8];

const users = [
    {
        name: 'Maria',
        age: 25
    },
    {
        name:'Ferran',
        age: 30
    },
    {
        name: 'Rafa',
        age: 35
    },
]

// console.log(numbers.reduce((cv, num, index, array) => cv += num, 0));

// console.log(users.reduce(
//     (cv, num, index, array) => cv += num.age, 0
// ));
// users.reduce(() => {}, 0);
